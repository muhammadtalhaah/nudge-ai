# dependencies
node_modules/

# build output
dist/
build/

# environment — never commit real secrets, only .env.example
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*
logs/

# test / coverage
coverage/
.vitest/

# editor + OS
.DS_Store
.idea/
*.swp
.vscode/*
!.vscode/extensions.json

.claude
.smoke-shots/
# Anchored to the repo root on purpose: an unanchored `scripts` also matches
# server/scripts/, which holds db:setup and db:seed — the deployment needs those.
/scripts/
.editorconfig
Dockerfile
CLAUDE.md
